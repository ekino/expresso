import { Options } from './definitions';
export declare const defaultOptions: Options;
export declare const runInValidEnvironment: (env: string, options?: Options) => boolean;
export declare const isEnable: (req: any, env: string, options?: any) => boolean;
